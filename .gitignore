# Live credentials — never commit these
zoho-config.php
zoho-token-cache.json

# Separate WordPress site, not part of this static deploy
campaign/

# Zip archives exceed GitHub's 100MB file limit; content already exists
# unzipped elsewhere in the repo
assets.zip
campaign.zip

# Unused backup/scratch content — not referenced by any live page, but
# was bloating every Vercel deployment (see storage cleanup 2026-09-23)
backup_21st_jan_2020/
modified_business-and-leisure.html
seo-backup-2026-09-11/

# Raw, full-resolution source photography (individual files up to 91MB).
# Only the reciprocals1-4.png files here are actually used on the site
# (by partnerships.html); everything else is an unoptimized original.
assets/A glimpse inside indus images/*
!assets/A glimpse inside indus images/reciprocals1.png
!assets/A glimpse inside indus images/reciprocals2.png
!assets/A glimpse inside indus images/reciprocals3.png
!assets/A glimpse inside indus images/reciprocals4.png
